version: '3'

includes:
  mockery: ./tools/mockery.yml
  protoc-gen: ./tools/protoc-gen.yml

tasks:
  default:
    desc: Run all generate tasks
    cmds:
      - task: protoc-gen
      - task: mockery
