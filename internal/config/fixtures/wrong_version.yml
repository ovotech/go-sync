version: '2'

jobs:
  - adapter:
      plugin: ./fixtures/valid.yml
      config:
        foo: bar
    with:
      - plugin: ./fixtures/valid.yml
        config:
          some: config
      - plugin: ./fixtures/valid.yml
        config:
          more: config
  - adapter:
      plugin: ./fixtures/valid.yml
    options:
      operatingMode: add
    with:
      - plugin: ./fixtures/valid.yml
        config:
          some: config
